# Westie Codex pet

`active-westie/` is the current Codex-compatible v2 package. Its spritesheet is the manually corrected version with clean transparent-pixel RGB data.

`history/` preserves prior installed/exported versions. `resources/` keeps the dog photo reference, the uncleaned Photoshop export, and the validation record for the cleaned active asset.
